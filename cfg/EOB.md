# EOB (End of Business) — 預設訂票參數

## 預設值

| 參數 | CLI Flag | 值 |
|---|---|---|
| 起程站 | `-f` | `5` (Hsinchu 新竹) |
| 到達站 | `-t` | `1` (Nangang 南港) |
| 出發時間 | `-T` | `25` (17:00) |
| 成人票數 | `-a` | `1` |
| 身分證字號 | `-i` | `Q122821678` |
| 高鐵會員 | `-m` | `y` |
| 手機號碼 | `-p` | `0954747258` |

## 執行指令

```bash
.venv/bin/python -m thsr_ticket.main \
  -f 5 -t 1 -T 25 \
  -i "Q122821678" \
  -m y \
  -p 0954747258
```

> 日期 `-d` 未設預設值，每次執行仍需輸入。
